import React from 'react';
import { BasicThemeUsage } from './product-context-context.composition';
import { render } from '@testing-library/react';

it('should render the button in the color blue', () => {
  expect(true).toBeTruthy();
});
